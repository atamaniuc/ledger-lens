# Work tracks

Index of live tracks (handoff-driven development). One line per track: essence — handoff — status, date, next step. Statuses: active / paused (+ why) / blocked (+ on what). Closed tracks → [TRACKS-LOG.md](TRACKS-LOG.md). Protocol: AGENTS.md → "Work tracks and handoffs"; spec 0015.

`task check` verifies this file: every link must resolve and every line must carry a status (src/platform/docs-proof.ts → checkTracks).

- **Hosted deploy** — none — blocked (on VERCEL_API_TOKEN), next: task infra-up → scripts/provision-hosted.sh → manual QA on hosted URL

