# The commit from which the locked-surface rule applies (harnessimo.config.json →
# locked.baseline).
#
# Moved forward once, deliberately: adopting Harnessimo required switching
# .github/workflows/ onto the shared CLI, and the workflow directory is itself
# locked. Widening what an agent may change is a human decision, and this line
# is where that decision is recorded.
ddd09d07f2cbeb1765ddf5b9e57fdc73e4ca3a36
